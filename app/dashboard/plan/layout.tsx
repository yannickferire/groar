import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Plan — Manage your subscription",
};

export default function PlanLayout({ children }: { children: React.ReactNode }) {
  return children;
}
